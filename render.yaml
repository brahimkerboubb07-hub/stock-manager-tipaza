services:
  - type: web
    name: stock-manager-tipaza
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: tipaza-db
          property: connectionString
